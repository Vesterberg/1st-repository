#include <iostream>
using namespace std;

int main()
{
    int size;
    cin >>size;
    int* x;
    x = new int[size];

    for(int i = 0; i < 5; i++) //changes array element per loop
    {
        cout << "Input text for element nr " << i << ": ";
        cin >> name[i];
        cout << name[i] << endl;
    }

    delete[] x;


    char text;
    cin >> text;
    char name = text;
    //Creates array:
    strlen(char name)
    cout << strlen(char* name) << endl;
    char name[];

    //sum the elements of the array and print it out in the terminal

    cin.get();
    return 0;
}







